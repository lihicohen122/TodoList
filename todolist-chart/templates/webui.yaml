apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.webui.name }}
spec:
  replicas: {{ .Values.webui.replicas | default .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.webui.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.webui.name }}
    spec:
      initContainers:
      - name: wait-for-db
        image: busybox:1.28
        command: ['sh', '-c', 'until nslookup {{ .Values.database.name }}-headless; do echo waiting for database; sleep 2; done;']
      containers:
      - name: webui
        image: {{ .Values.webui.image }}
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: {{ .Values.webui.name }}-config
        - secretRef:
            name: {{ .Values.webui.name }}-secret
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.webui.name }}-svc
spec:
  type: {{ .Values.webui.serviceType | default "LoadBalancer" }}
  selector:
    app: {{ .Values.webui.name }}
  ports:
  - port: 8080
    targetPort: 8080

