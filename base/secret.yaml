apiVersion: v1
kind: Secret
metadata:
  name: todolist-secret
type: Opaque
data:
  # MYSQL_ROOT_PASSWORD must be set via kustomize or kubectl
  # Example: echo -n 'your-password' | base64
  MYSQL_ROOT_PASSWORD: ""  # Set via kustomize secretGenerator or kubectl 